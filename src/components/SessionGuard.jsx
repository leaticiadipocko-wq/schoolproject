/**
 * Session Guard - Inactivity monitoring DISABLED
 * The system no longer enforces automatic logout due to inactivity.
 * Users can stay signed in indefinitely.
 */

export default function SessionGuard() {
  // Inactivity monitoring disabled - users can stay signed in indefinitely
  return null
}